<?xml version="1.0" encoding="utf-8"?>
<ripple xmlns:android="http://schemas.android.com/apk/res/android"
    android:color="?attr/colorControlHighlight">

    <item>
        <shape android:shape="rectangle">
            <!-- Fundo branco -->
            <solid android:color="#FFFFFFFF" />

            <!-- Borda azul clara -->
            <stroke
                android:width="2dp"
                android:color="#1d405c" />

            <!-- Cantos arredondados -->
            <corners android:radius="5dp" />

            <!-- Leve sombra (usando padding e elevation no layout) -->
            <padding
                android:left="6dp"
                android:top="6dp"
                android:right="6dp"
                android:bottom="6dp" />
        </shape>
    </item>
</ripple>
